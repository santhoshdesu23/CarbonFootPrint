import Card from '../common/Card';

export default function SolutionSection() {
  return (
    <Card>
      <h2 className="text-xl font-semibold text-slate-900">The solution</h2>
      <p className="mt-2 text-sm text-slate-600">A unified experience that combines footprint tracking, smart recommendations, and progress visualization in one premium interface.</p>
    </Card>
  );
}
